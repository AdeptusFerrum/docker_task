# Docker Task — Static HTML Page

Этот проект демонстрирует, как запустить простую статическую HTML-страницу в Docker с использованием Nginx.

## 📁 Структура проекта

```
docket_task/
 ├── index.html
 └── Dockerfile
```

- **index.html** — статическая страница  
- **Dockerfile** — описание Docker-образа

---

## 🛠️ Используемые технологии

- **Docker**
- **Nginx** (в качестве веб-сервера)

---

## 🚀 Как запустить проект в Docker

### 1. Собрать Docker-образ

Открыть терминал в каталоге проекта и выполнить команду:

```bash
docker build -t mypage .
```

### 2. Запустить контейнер

```bash
docker run -d -p 8080:80 mypage
```

### 3. Открыть страницу

После запуска контейнера страница будет доступна по адресу:

http://localhost:8080

---

## 📝 Описание Dockerfile

```Dockerfile
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
```

- Используется официальный образ **nginx:alpine**
- Файл `index.html` копируется в директорию, которую Nginx использует для статических файлов

---

## 📦 Остановка контейнера

Чтобы увидеть запущенные контейнеры:

```bash
docker ps
```

Чтобы остановить контейнер:

```bash
docker stop <container_id>
```
